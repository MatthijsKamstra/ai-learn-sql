Topic: "${topic}"

Task: Write a full tutorial using template EXACTLY. Fill every section. Keep each explanation concise. Use only SQLite features. Reuse demo schema. No extra sections, no intro/outro outside template.

Template:
${template}
